package banking.core;

// This enum is used to detect the file type in reading and updating files
public enum EntityType {
    Account,
    AccountRequest,
    Client,
    Employee,
    Transaction,
    TransactionRequest
}
